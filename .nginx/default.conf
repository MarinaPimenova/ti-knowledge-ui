server {
        server_tokens off;
        listen 80;

        root /usr/share/nginx/html/;
        index index.html;

        # Serve the React app for /knowledge-ui/
location /dashboard-page/ {
    alias /usr/share/nginx/html/dashboard-page/;
    try_files $uri $uri/ /dashboard-page/index.html;
}


        # Enable gzip compression
        gzip            on;
        gzip_vary       on;
        gzip_http_version 1.0;
        gzip_comp_level 5;
        gzip_types
                        application/javascript
                        application/json
                        text/css
                        text/plain
                        text/xml
                        image/svg+xml;
        gzip_proxied    no-cache no-store private expired auth;
        gzip_min_length 256;

        # Handle 404 errors by serving the index.html page
        error_page 404 /error.html;
}
